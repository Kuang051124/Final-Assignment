# 工业机器人视觉引导系统 — 项目指引

## 一、项目概述

在仿真环境中开发一个工业机器人视觉引导系统，实现：**仿真相机采集图像 → 图像处理检测工件 → 3D位姿还原 → 路径规划 → 驱动机器人抓取并搬运**的完整闭环。

**仿真平台**：PyBullet（`pip install pybullet`），免费、轻量、原生 Python 接口。

**核心架构**：

```
PyBullet 虚拟场景
   │
   ├─相机渲染 RGB 图──→ OpenCV 图像处理 ──→ 工件检测(类别+像素坐标)
   │                                              │
   │                                              ▼
   │                                         深度缓冲 + 反投影
   │                                              │
   │                                              ▼
   │                                         工件 3D 世界坐标
   │                                              │
   │                                              ▼
   │                                         抓取规划 + 门型路径
   │                                              │
   │                                              ▼
   └─机器人 IK + 关节控制 ←────────────────── 目标位姿
```

整个管线从 PyBullet 获取图像，最后将指令发回 PyBullet 驱动机器人，形成**感知 → 规划 → 执行**的闭环。

### 为什么用仿真而不是实物

| 对比维度 | 实物方案 | PyBullet 仿真 |
|----------|---------|--------------|
| 硬件需求 | 摄像头、棋盘格、打印标记、机器人 | 无（一台电脑即可） |
| 检测难度 | 光照/阴影/反光/背景干扰严重 | 颜色纯净、光照可控、简化后可靠 |
| 深度获取 | 需额外传感器或估计算法 | 深度缓冲一行代码拿到真值 |
| 定量评估 | 无 ground truth，只能定性描述 | 每个物体的真实位姿可查，逐项算误差 |
| 演示稳定性 | 受环境光线影响可能翻车 | 每次运行结果一致，可复现 |

---

## 二、技术栈

| 模块 | 工具/库 |
|------|--------|
| 编程语言 | Python 3.8+ |
| 虚拟环境 | conda（环境名：`Picture-Processing`） |
| 仿真引擎 | PyBullet |
| 图像处理 | OpenCV (cv2) |
| 数值计算 | NumPy |
| 可视化 | Matplotlib |
| 可选：GUI | PyQt5 / Tkinter |

---

## 三、分步实施计划

### 第 1 步：环境搭建与 PyBullet 场景构建

**目标**：激活 conda 环境，安装依赖，构建包含桌面、工件、机器人、相机的虚拟场景。

- [ ] 激活环境并安装 PyBullet：
  ```bash
  conda activate Picture-Processing
  pip install pybullet
  ```
- [ ] 验证环境：
  ```bash
  python -c "import pybullet; import cv2; import numpy; print('OK')"
  ```
- [ ] 创建项目目录：
  ```
  project/
  ├── src/
  │   ├── scene.py           # 场景搭建
  │   ├── camera.py          # 虚拟相机
  │   ├── detection.py       # 目标检测
  │   ├── depth_pose.py      # 深度+位姿还原
  │   ├── grasp_plan.py      # 抓取规划+路径
  │   ├── main.py            # 完整闭环运行
  │   └── utils.py           # 工具函数
  └── output/                # 运行截图/结果
  ```
- [ ] 搭建 PyBullet 场景（`scene.py`）：
  - 加载地面平面
  - 放置一个桌面（灰/白色平板）
  - 放置 3-4 个工件（用不同颜色的方块/柱体代表不同零件，如：红色齿轮、蓝色轴承、绿色垫片）
  - 加载一个工业机器人（KUKA iiwa 或 UR5）
  - 在机器人上方设置一个虚拟相机

```python
import pybullet as p
import pybullet_data

def build_scene():
    p.connect(p.GUI)  # 或 p.DIRECT 用于无界面跑批
    p.setAdditionalSearchPath(pybullet_data.getDataPath())
    p.setGravity(0, 0, -9.8)

    # 地面
    p.loadURDF("plane.urdf")

    # 桌面
    table = p.loadURDF("table/table.urdf", [0.5, 0, 0])

    # 机器人：KUKA iiwa
    robot = p.loadURDF("kuka_iiwa/model.urdf", [0, 0, 0.6])

    # 工件：用彩色基本几何体
    # 红色工件 — 模拟齿轮
    obj1 = p.createMultiBody(
        baseMass=0.5,
        baseCollisionShapeIndex=p.createCollisionShape(
            p.GEOM_CYLINDER, radius=0.03, height=0.02),
        baseVisualShapeIndex=p.createVisualShape(
            p.GEOM_CYLINDER, radius=0.03, length=0.02,
            rgbaColor=[1, 0, 0, 1]),
        basePosition=[0.6, -0.1, 0.63])

    # 蓝色工件 — 模拟轴承
    obj2 = p.createMultiBody(
        baseMass=0.3,
        baseCollisionShapeIndex=p.createCollisionShape(
            p.GEOM_BOX, halfExtents=[0.025, 0.025, 0.01]),
        baseVisualShapeIndex=p.createVisualShape(
            p.GEOM_BOX, halfExtents=[0.025, 0.025, 0.01],
            rgbaColor=[0, 0, 1, 1]),
        basePosition=[0.55, 0.1, 0.63])

    return robot, [obj1, obj2]
```

**交付物**：运行后能在 PyBullet GUI 中看到桌面、工件、机器人臂的 3D 场景。

---

### 第 2 步：虚拟相机与图像采集

**目标**：从仿真相机获取 RGB 图像和深度图，转为 OpenCV 可处理的格式。

- [ ] 在场景中设置虚拟相机（`camera.py`）：
  - 固定相机在机器人上方，俯视桌面
  - 计算 `viewMatrix` 和 `projectionMatrix`
- [ ] 调用 `p.getCameraImage()` 渲染 RGB 图像
- [ ] 将 RGBA 转为 BGR（OpenCV 格式）
- [ ] 提取深度缓冲（`depth_buffer`，单位：米）
- [ ] 从 `projectionMatrix` 推算内参矩阵 K（fx, fy, cx, cy）

```python
import numpy as np
import cv2

class SimCamera:
    def __init__(self, width=640, height=480, fov=60, near=0.01, far=5.0):
        self.width = width
        self.height = height
        self.fov = fov
        self.near = near
        self.far = far
        self._compute_intrinsics()

    def _compute_intrinsics(self):
        """从 FOV 和图像尺寸计算内参矩阵（无畸变）"""
        fx = self.width / (2 * np.tan(np.radians(self.fov) / 2))
        fy = fx  # 方形像素
        cx = self.width / 2
        cy = self.height / 2
        self.K = np.array([[fx, 0, cx],
                           [0, fy, cy],
                           [0,  0,  1]])

    def get_view_matrix(self, cam_pos, target_pos):
        """计算 viewMatrix：相机在 cam_pos 看向 target_pos"""
        up = [0, 0, 1]
        return p.computeViewMatrix(cam_pos, target_pos, up)

    def get_proj_matrix(self):
        aspect = self.width / self.height
        return p.computeProjectionMatrixFOV(
            self.fov, aspect, self.near, self.far)

    def capture(self, cam_pos, target_pos):
        """捕获一帧，返回 RGB 图和深度图"""
        vm = self.get_view_matrix(cam_pos, target_pos)
        pm = self.get_proj_matrix()
        img = p.getCameraImage(self.width, self.height, vm, pm)
        rgb = np.reshape(img[2], (self.height, self.width, 4))[:, :, :3]
        depth = np.reshape(img[3], (self.height, self.width))  # 单位：米
        return rgb.astype(np.uint8), depth
```

**交付物**：能从仿真相机获取 RGB 图 + 深度图，深度图已校准为米单位。

---

### 第 3 步：目标检测

**目标**：从 RGB 图像中检测工件，输出每个工件的类别、像素坐标、旋转角。

**策略**：仿真环境下工件颜色固定且纯净，使用 **HSV 颜色分割 + 轮廓检测** 即可可靠工作。

- [ ] 将 BGR 图像转到 HSV 空间
- [ ] 为每种工件定义 HSV 颜色范围（如红色 [0,100,100]~[10,255,255]）
- [ ] 用 `cv2.inRange()` 逐颜色分割
- [ ] 形态学开运算去噪
- [ ] `cv2.findContours()` 提取轮廓
- [ ] 按面积筛选，排除小噪声
- [ ] `cv2.minAreaRect()` 获取中心坐标 `(cx, cy)` 和旋转角
- [ ] 颜色范围映射到工件类别（如：红色区域 → "齿轮"）

```python
import cv2
import numpy as np

# 定义每个工件的 HSV 范围
CLASS_HSV = {
    "gear":      (np.array([0,   100, 100]), np.array([10,  255, 255])),  # 红色
    "bearing":   (np.array([100, 100, 100]), np.array([130, 255, 255])),  # 蓝色
    "washer":    (np.array([40,  100, 100]), np.array([80,  255, 255])),  # 绿色
}

class DetectedObject:
    def __init__(self, class_name, cx, cy, angle, w, h, contour):
        self.class_name = class_name
        self.cx = cx          # 像素中心 x
        self.cy = cy          # 像素中心 y
        self.angle = angle    # 旋转角（度）
        self.width_px = w     # 像素宽度
        self.height_px = h    # 像素高度
        self.contour = contour

def detect_objects(rgb_img):
    hsv = cv2.cvtColor(rgb_img, cv2.COLOR_RGB2HSV)
    results = []

    for class_name, (lower, upper) in CLASS_HSV.items():
        mask = cv2.inRange(hsv, lower, upper)
        # 形态学开运算去噪
        kernel = cv2.getStructuringElement(cv2.MORPH_ELLIPSE, (5, 5))
        mask = cv2.morphologyEx(mask, cv2.MORPH_OPEN, kernel)

        contours, _ = cv2.findContours(mask, cv2.RETR_EXTERNAL,
                                        cv2.CHAIN_APPROX_SIMPLE)
        for cnt in contours:
            area = cv2.contourArea(cnt)
            if area < 200:  # 面积阈值，排除噪声
                continue
            rect = cv2.minAreaRect(cnt)  # (cx, cy), (w, h), angle
            obj = DetectedObject(
                class_name=class_name,
                cx=rect[0][0], cy=rect[0][1],
                angle=rect[2],
                w=rect[1][0], h=rect[1][1],
                contour=cnt)
            results.append(obj)

    return results
```

**交付物**：对每帧图像输出工件列表 `[(class_name, cx, cy, angle), ...]`。

---

### 第 4 步：深度获取与 3D 位姿还原

**目标**：利用深度缓冲和内参矩阵，将检测到的 2D 像素坐标反投影为 3D 世界坐标。

- [ ] 从深度缓冲中读取工件中心像素的深度值 Zc
- [ ] 反投影到相机坐标系：`Xc = (cx - K[0,2]) * Zc / K[0,0]`, `Yc = (cy - K[1,2]) * Zc / K[1,1]`
- [ ] 获取相机在世界坐标系中的位姿（`p.getBasePositionAndOrientation`）
- [ ] 将相机坐标系下的点变换到世界坐标系
- [ ] 对比：PyBullet 可查询物体真实世界坐标，用于验证算法精度

```python
def pixel_to_camera(cx, cy, depth, K):
    """像素坐标 + 深度 → 相机坐标系 3D 点"""
    Xc = (cx - K[0, 2]) * depth / K[0, 0]
    Yc = (cy - K[1, 2]) * depth / K[1, 1]
    Zc = depth
    return np.array([Xc, Yc, Zc])

def camera_to_world(pt_cam, cam_pos, cam_orn_quat):
    """相机坐标系 → 世界坐标系"""
    rot_mat = np.array(p.getMatrixFromQuaternion(cam_orn_quat)).reshape(3, 3)
    return rot_mat @ pt_cam + np.array(cam_pos)

def restore_3d_position(obj, depth_img, K, cam_pos, cam_orn):
    """从检测结果恢复物体 3D 世界坐标"""
    # 取物体中心周围 5×5 区域的深度中值（鲁棒）
    cx, cy = int(obj.cx), int(obj.cy)
    r = 2
    depth_patch = depth_img[cy-r:cy+r+1, cx-r:cx+r+1]
    depth_val = np.median(depth_patch[depth_patch > 0])  # 过滤 0 值
    if depth_val <= 0:
        return None

    pt_cam = pixel_to_camera(obj.cx, obj.cy, depth_val, K)
    pt_world = camera_to_world(pt_cam, cam_pos, cam_orn)
    return pt_world  # (Xw, Yw, Zw) 单位：米
```

**深度校验**（定量评估用）：
```python
# PyBullet 查询物体真实位置（ground truth）
real_pos, _ = p.getBasePositionAndOrientation(obj_id)
error_3d = np.linalg.norm(calculated_pos - np.array(real_pos))
print(f"3D 定位误差: {error_3d*1000:.1f} mm")  # 期望 < 5mm
```

**交付物**：每个工件输出世界坐标 (X, Y, Z)，含与 ground truth 的定量误差。

---

### 第 5 步：抓取规划与机器人路径生成

**目标**：根据工件世界坐标，规划门型抓取路径，用逆运动学求解关节角。

- [ ] 定义抓取点 = 工件中心 + 抓取高度偏置
- [ ] 门型路径：安全高位 → 接近点 → 抓取点 → 抬起 → 放置点上方 → 放置点
- [ ] 放置点设为场景中另一固定位置（模拟搬运目的地的料箱/传送带）
- [ ] 用 `p.calculateInverseKinematics()` 对每个路径点求解关节角
- [ ] 在渲染画面上叠加路径可视化（`cv2.arrowedLine`）

```python
GRIP_OFFSET = np.array([0, 0, 0.02])   # 夹爪到物体中心的偏移
SAFE_Z = 0.85                            # 安全运动高度
PLACE_POS = np.array([0.3, 0.3, 0.64])  # 放置点

def plan_grasp_path(obj_world_pos):
    """生成门型抓取+搬运路径点序列"""
    approach = obj_world_pos + np.array([0, 0, 0.1])  # 接近点
    grasp   = obj_world_pos + GRIP_OFFSET               # 抓取点
    retreat = obj_world_pos + np.array([0, 0, 0.1])     # 抬起
    above_place = PLACE_POS + np.array([0, 0, 0.1])    # 放置上方
    place   = PLACE_POS + np.array([0, 0, -0.05])       # 放置
    return [approach, grasp, retreat, above_place, place]

def solve_ik(robot_id, target_pos):
    """对给定位姿求解关节角（末端保持垂直向下）"""
    end_effector_idx = 6  # KUKA iiwa 末端关节
    joint_angles = p.calculateInverseKinematics(
        robot_id,
        end_effector_idx,
        target_pos,
        maxNumIterations=100,
        residualThreshold=1e-4)
    return joint_angles[:7]  # KUKA 有 7 个关节
```

**加分项 — A\* 避障路径**：
- 在俯视图 2D 平面做 A\* 网格搜索
- 障碍物在网格中标记为不可通过
- 生成绕开障碍物的路径替代直线水平段

**交付物**：给定一个工件的世界坐标，输出一组关节角序列。

---

### 第 6 步：系统集成与闭环演示

**目标**：将所有模块串成实时循环，展示感知→规划→执行的完整闭环。

#### 6.1 完整主循环（`main.py`）

```python
import pybullet as p
from camera import SimCamera
from detection import detect_objects
from depth_pose import restore_3d_position
from grasp_plan import plan_grasp_path, solve_ik

def main():
    p.connect(p.GUI)
    robot, obj_ids = build_scene()
    cam = SimCamera(width=640, height=480)

    cam_pos = [0.5, 0, 1.2]     # 相机位置（机器人上方俯视）
    target_pos = [0.5, 0, 0.6]  # 看向桌面中心

    for step in range(1000):
        # 1. 采集图像
        rgb, depth = cam.capture(cam_pos, target_pos)

        # 2. 目标检测
        objects = detect_objects(rgb)

        # 3. 深度 + 3D 位姿
        cam_orn = p.getQuaternionFromEuler([0, np.radians(45), 0])
        for obj in objects:
            obj.world_pos = restore_3d_position(
                obj, depth, cam.K, cam_pos, cam_orn)

        # 4. 抓取其中一个物体（取第一个检测到的）
        if objects:
            target = objects[0]
            if target.world_pos is not None:
                # 5. 规划路径 + IK 求解
                path = plan_grasp_path(target.world_pos)
                # 6. 驱动关节
                for waypoint in path:
                    joints = solve_ik(robot, waypoint)
                    p.setJointMotorControlArray(
                        robot, range(7), p.POSITION_CONTROL,
                        targetPositions=joints)
                    # 每步仿真推进足够时间
                    for _ in range(120):
                        p.stepSimulation()

        # 可视化叠加（检测框、坐标、路径）
        viz = draw_overlay(rgb, objects)
        cv2.imshow("Robot Vision", cv2.cvtColor(viz, cv2.COLOR_RGB2BGR))
        if cv2.waitKey(1) & 0xFF == 27:
            break

    cv2.destroyAllWindows()
    p.disconnect()
```

#### 6.2 可视化叠加（`draw_overlay`）

在渲染图上叠加以下内容：

| 叠加元素 | 画法 |
|---------|------|
| 检测框 | `cv2.drawContours` — 绿色轮廓 |
| 类别标签 | `cv2.putText` — 物体名称 |
| 像素坐标 | `cv2.putText` — `uv=(cx, cy)` |
| 3D 世界坐标 | `cv2.putText` — `XYZ=(x,y,z) mm` |
| 门型路径 | `cv2.arrowedLine` — 蓝色箭头，从相机位置连到抓取点 |
| 抓取点标记 | `cv2.circle` — 红色圆点 |
| 底部状态栏 | `FPS`、`检出数` |

#### 6.3 定量评估

在报告中逐项列出：

| 指标 | 计算方法 |
|------|---------|
| 检测精度 | 像素中心坐标误差 vs 投影真值 |
| 3D 定位误差 | 欧氏距离 `||P_calc - P_gt||`（mm） |
| 角度误差 | `|angle_calc - angle_gt|`（度） |
| 抓取成功判定 | PyBullet 物理模拟中工件是否被抓起并搬运 |
| 路径避障 | 路径是否绕过障碍物 |

---

## 四、时间规划

| 阶段 | 内容 | 建议时间 |
|------|------|---------|
| 第1-2天 | 环境搭建、PyBullet 场景构建（步骤1） | 1-2天 |
| 第3-4天 | 虚拟相机 + 图像获取（步骤2） | 2天 |
| 第5-7天 | 目标检测 HSV 分割 + 轮廓（步骤3） | 2-3天 |
| 第7-9天 | 深度获取 + 3D 坐标还原 + 误差验证（步骤4） | 2-3天 |
| 第9-12天 | 路径规划 + IK 控制（步骤5） | 3天 |
| 第12-14天 | 系统集成 + 可视化 + 定量评估 + 报告（步骤6） | 3天 |

---

## 五、关键技术参考

| 技术点 | 关键 API |
|--------|---------|
| 场景构建 | `pybullet.connect`, `loadURDF`, `createMultiBody` |
| 相机渲染 | `getCameraImage`, `computeViewMatrix`, `computeProjectionMatrixFOV` |
| 颜色分割 | `cv2.cvtColor(RGB2HSV)`, `cv2.inRange`, `cv2.morphologyEx` |
| 轮廓检测 | `cv2.findContours`, `cv2.minAreaRect` |
| 深度反投影 | 内参矩阵 K + 深度值 → 相机坐标 |
| 坐标变换 | `p.getBasePositionAndOrientation`, 旋转矩阵 |
| 逆运动学 | `p.calculateInverseKinematics` |
| 关节控制 | `p.setJointMotorControlArray`, `p.stepSimulation` |

---

## 六、常见坑

1. **PyBullet URDF 路径** — Windows 上可能找不到模型文件，使用 `pybullet_data.getDataPath()` 确保路径正确
2. **深度缓冲单位** — PyBullet 深度缓冲单位是**米**，且值为相机坐标系下的 Z（不是欧氏距离）
3. **图像通道顺序** — PyBullet 返回 RGBA（uint8），OpenCV 用 BGR，`cv2.cvtColor` 前确保格式正确
4. **IK 多解** — 逆运动学可能有多组解，加关节限位约束避免奇异构型
5. **物理步长** — `p.stepSimulation()` 要和关节控制的时间步匹配，否则运动不完成
6. **相机遮挡** — 确保相机位置能看到桌面上的工件，避免被机器人臂遮挡
7. **红色 HSV 范围分两段** — 红色在 HSV 中 hue 在 0° 和 180° 两端，需要用两个范围做 `cv2.bitwise_or` 合并

---

从头到尾在一个 PyBullet 仿真循环里完成，每一步都有明确的输入输出和定量验证方式。
