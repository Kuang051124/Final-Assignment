# 工业机器人视觉引导系统 — 项目指引

## 一、项目概述

开发一个工业机器人视觉引导系统，核心流程为：

> **相机采集 → 图像预处理 → 目标检测与定位 → 位姿估计 → 坐标变换 → 路径规划 → 机器人抓取**

使用 **Python + OpenCV** 实现核心视觉算法。

### 演示策略：静态开发 + 实时展示

| 阶段 | 方式 | 说明 |
|------|------|------|
| **开发调试** | 静态图片 | 用手机拍摄的工件照片调试算法，方便复现和对比效果 |
| **最终演示** | USB 摄像头实时 | 连接摄像头实时采集画面，屏幕叠加可视化结果 |

实时演示时，画面需叠加显示：

- **检测框 + 类别标签** — 每个检出的工件画彩色包围框和名称
- **物体中心坐标** — 框旁标注像素坐标 `(u, v)` 和世界坐标 `(X, Y, Z)`
- **规划的抓取路径** — 用箭头/虚线绘制：安全高位 → 接近点 → 抓取点
- **深度估计值** — 在物体标记旁显示估算的深度距离

---

## 二、推荐技术栈

| 模块 | 推荐工具/库 |
|------|------------|
| 编程语言 | Python 3.8+ |
| 虚拟环境 | **conda**（环境名：`Picture-Processing`） |
| 图像处理 | OpenCV (cv2) |
| 数值计算 | NumPy |
| 可视化 | Matplotlib |
| 摄像头采集 | OpenCV `VideoCapture` |
| 可选：深度学习检测 | YOLO / torchvision |
| 可选：GUI | PyQt5 / Tkinter |

---

## 三、分步实施计划

### 第 1 步：环境搭建与数据准备

**目标**：搭好开发环境，准备好测试图像数据。

- [ ] **激活 conda 环境**（环境已预装依赖）：
  ```bash
  conda activate Picture-Processing
  ```
- [ ] 验证依赖可用：
  ```bash
  python -c "import cv2; import numpy; import matplotlib; print('OK')"
  ```
- [ ] 准备测试数据：
  - 从网上搜集工业零件/工件的图片（齿轮、轴承、螺丝等）
  - 或自己用手机拍摄若干物体照片（不同角度、不同光照）
  - 建议准备 20-50 张，包含不同位姿和背景
- [ ] 建立项目目录结构：
  ```
  project/
  ├── data/              # 原始图像
  ├── output/            # 中间结果输出
  ├── src/
  │   ├── calibration.py     # 相机标定
  │   ├── preprocessing.py   # 图像预处理
  │   ├── detection.py       # 目标检测
  │   ├── depth.py           # 深度估计
  │   ├── transform.py       # 坐标变换
  │   └── main.py            # 主流程整合（静态图片模式）
  │   └── demo_realtime.py   # 实时摄像头演示
  └── README.md
  ```

**交付物**：可运行的环境 + 测试图像集 + 项目骨架代码。

---

### 第 2 步：相机标定

**目标**：获取相机内参和畸变系数，消除镜头畸变。

**核心算法**：张正友棋盘格标定法。

- [ ] 打印一张棋盘格（9×6 内角点），用相机从不同角度拍摄 15-20 张
- [ ] 使用 `cv2.findChessboardCorners()` 检测角点
- [ ] 使用 `cv2.calibrateCamera()` 计算内参矩阵 K 和畸变系数 D
- [ ] 使用 `cv2.undistort()` 对后续所有图像做畸变校正
- [ ] 保存标定结果到 `.npz` 文件供后续使用

**关键 API**：
```python
ret, mtx, dist, rvecs, tvecs = cv2.calibrateCamera(objpoints, imgpoints, gray.shape[::-1], None, None)
dst = cv2.undistort(img, mtx, dist, None, mtx)
```

**交付物**：标定脚本 + 内参矩阵 + 畸变系数文件。

---

### 第 3 步：图像预处理

**目标**：增强图像质量，为检测和分割做准备。

需要实现的技术：

- [ ] **灰度化**：`cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)`
- [ ] **高斯滤波去噪**：`cv2.GaussianBlur()`，核大小 5×5
- [ ] **直方图均衡化**：增强对比度（光照不均环境下尤其重要）
- [ ] **Canny 边缘检测**：提取物体轮廓边缘
- [ ] **形态学操作**：膨胀/腐蚀连接断裂边缘，去除噪声

**注意事项**：
- 预处理参数应根据实际图像调试，不要写死
- 建议封装为 `preprocess(image, params)` 函数，方便调参

**交付物**：预处理模块 + 不同参数效果对比图。

---

### 第 4 步：目标检测与分割

**目标**：从图像中找到目标工件，分割出物体区域。

根据任务复杂度选择方案：

#### 方案 A：传统图像处理（推荐，课设够用）

- [ ] **阈值分割**：`cv2.threshold()` 或 `cv2.adaptiveThreshold()`
- [ ] **Otsu 大津法**：自动二值化，适合物体与背景对比度高的场景
- [ ] **HSV 颜色空间分割**：如果物体颜色固定，用 `cv2.inRange()` 按颜色筛选
- [ ] **轮廓提取**：`cv2.findContours()` 获取物体轮廓
- [ ] **轮廓筛选**：按面积/长宽比/圆度过滤，排除噪声和无关物体
- [ ] **最小外接矩形**：`cv2.minAreaRect()` 获取物体包围盒和旋转角

#### 方案 B：深度学习检测（加分项）

- [ ] 使用预训练 YOLO 或 SSD 模型
- [ ] 或自行标注少量数据微调

**关键代码片段**：
```python
# 轮廓提取示例
contours, _ = cv2.findContours(binary, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
for cnt in contours:
    area = cv2.contourArea(cnt)
    if area > min_area:
        rect = cv2.minAreaRect(cnt)  # 获取中心(x,y)、宽高、旋转角
        box = cv2.boxPoints(rect)
```

**交付物**：检测模块 + 标注了检测框的结果图。

---

### 第 5 步：图像校正（透视变换）

**目标**：当相机不是正对物体时，通过透视变换将物体"摆正"，便于精确抓取。

- [ ] 检测物体的 4 个角点（或使用最小外接矩形的 4 个顶点）
- [ ] 定义目标矩形（物体在正视图下的理想尺寸）
- [ ] 使用 `cv2.getPerspectiveTransform()` 计算透视变换矩阵
- [ ] 使用 `cv2.warpPerspective()` 执行透视校正
- [ ] 校正后重新计算物体的精确中心坐标和角度

**关键 API**：
```python
M = cv2.getPerspectiveTransform(src_pts, dst_pts)
corrected = cv2.warpPerspective(img, M, (width, height))
```

**交付物**：透视校正模块 + 校正前后对比图。

---

### 第 6 步：深度信息获取

**目标**：获取物体的深度（Z 坐标），实现三维定位。

根据硬件条件选择：

#### 方案 A：单目测距（无深度相机时）

- [ ] **相似三角形法**：已知物体实际尺寸，利用焦距和成像尺寸估算距离
  ```
  Z = (f * real_size) / pixel_size
  ```
- [ ] **二维码/ArUco 标记**：放置已知尺寸的标记，利用 `cv2.aruco` 估计位姿

#### 方案 B：双目立体视觉

- [ ] 使用两个相机（或同一相机两个位置）拍摄
- [ ] 立体标定：`cv2.stereoCalibrate()`
- [ ] 立体匹配：`cv2.StereoSGBM_create()` 计算视差图
- [ ] 深度计算：`Z = (f * B) / disparity`（B 为基线距离）

#### 方案 C：RGB-D 相机（如 RealSense / Kinect）

- [ ] 直接读取深度图，对齐到彩色图

**建议**：课设环境下，用**方案 A（ArUco 标记 + 相似三角形）**最实际，不需要特殊硬件。

**交付物**：深度估计模块 + 深度计算结果。

---

### 第 7 步：手眼标定与坐标变换

**目标**：将相机坐标系下的物体位姿转换到机器人基坐标系。

- [ ] 理解四个坐标系关系：
  - 世界坐标系 / 机器人基坐标系
  - 相机坐标系
  - 图像坐标系（像素）
  - 物体坐标系

- [ ] **图像坐标 → 相机坐标**（利用内参矩阵 K 和深度 Z）：
  ```
  Xc = (u - cx) * Z / fx
  Yc = (v - cy) * Z / fy
  Zc = Z
  ```

- [ ] **相机坐标 → 机器人坐标**（手眼变换矩阵）：
  - 如果无法做真正的手眼标定，可简化为测量相机与机器人的相对位置，定义一个固定的平移+旋转变换矩阵

**交付物**：坐标变换模块。

---

### 第 8 步：抓取位姿计算

**目标**：计算机器人末端执行器应到达的位置和姿态。

- [ ] **抓取点** = 物体中心坐标（在机器人基坐标系下）
- [ ] **抓取角度** = 物体最小外接矩形的旋转角 + 末端夹爪偏置
- [ ] **接近点** = 抓取点上方安全高度（Z + offset）
- [ ] 定义抓取流程的关键路径点：
  ```
  起始安全位 → 接近点（物体上方）→ 抓取点 → 抬起 → 放置点上方 → 放置点 → 退回
  ```

**交付物**：抓取位姿计算模块。

---

### 第 9 步：路径规划

**目标**：生成从当前位置到目标位置的无碰撞路径。

- [ ] **点到点运动**：最简单，直接直线插补
- [ ] **梯形速度规划**：加速—匀速—减速，生成平滑轨迹
- [ ] **避障**（加分项）：
  - 使用 A* 或 RRT 算法在二维平面规划路径
  - 若为关节空间路径，可用多项式插值平滑关节角度

**简化方案**（推荐课设先用这个）：
- 将路径分解为：安全高度水平移动 → 垂直下降 → 抓取 → 垂直上升 → 水平移动至放置点 → 垂直下降放置

**交付物**：路径规划模块 + 路径可视化图。

---

### 第 10 步：系统集成与演示

**目标**：将所有模块串成完整流程，分两种模式演示。

#### 10.1 静态图片模式（`main.py`）— 开发调试用

加载本地图片，依次调用所有模块，输出中间结果图：

```python
def main():
    img = load_image()
    img = undistort(img, mtx, dist)        # 步骤2
    img = preprocess(img)                   # 步骤3
    objects = detect_objects(img)           # 步骤4
    for obj in objects:
        corrected = perspective_correct(img, obj)  # 步骤5
        depth = estimate_depth(obj)                # 步骤6
        robot_pos = camera_to_robot(obj, depth)    # 步骤7
        grasp = compute_grasp(robot_pos)           # 步骤8
        path = plan_path(grasp)                    # 步骤9
        draw_result(img, obj, depth, path)         # 叠加可视化
        save_output(img)
```

#### 10.2 实时摄像头模式（`demo_realtime.py`）— 最终演示用

连接 USB 摄像头（笔记本自带也可），逐帧处理，在画面上实时叠加以下信息：

**画面叠加内容：**

1. **检测框 + 类别标签** — `cv2.drawContours()` 画轮廓，`cv2.putText()` 标注类别名
2. **物体中心坐标** — 标注 `(u,v)` 像素坐标和换算后的 `(X,Y,Z)` 世界坐标
3. **深度估计值** — 标注 `Z = xxx mm`
4. **抓取路径** — 用 `cv2.arrowedLine()` 画带箭头的线段，示意：安全高位 → 接近点 → 抓取点
5. **状态栏** — 画面底部显示 FPS、检出物体数量、当前模式

**实时主循环框架：**

```python
def demo_realtime():
    cap = cv2.VideoCapture(0)  # 打开摄像头
    cap.set(cv2.CAP_PROP_FRAME_WIDTH, 1280)
    cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 720)

    while True:
        ret, frame = cap.read()
        if not ret:
            break

        frame = cv2.undistort(frame, mtx, dist, None, mtx)

        objects = detect_objects(frame)
        for obj in objects:
            depth = estimate_depth(obj)
            robot_pos = camera_to_robot(obj, depth)
            path = plan_path(compute_grasp(robot_pos))

            # 画检测框
            cv2.drawContours(frame, [obj.box.astype(int)], 0, (0,255,0), 2)
            # 写类别标签
            cv2.putText(frame, obj.label, tuple(obj.box[0].astype(int)),
                        cv2.FONT_HERSHEY_SIMPLEX, 0.7, (0,255,0), 2)
            # 写坐标
            cv2.putText(frame, f"uv=({obj.cx:.0f},{obj.cy:.0f})",
                        (obj.cx+10, obj.cy-20), ...)
            cv2.putText(frame, f"XYZ=({robot_pos[0]:.0f},{robot_pos[1]:.0f},{depth:.0f})mm",
                        (obj.cx+10, obj.cy), ...)
            # 画抓取路径箭头
            for i in range(len(path)-1):
                cv2.arrowedLine(frame, path[i][:2], path[i+1][:2],
                                (255,0,0), 2, tipLength=0.1)

        # 底部状态栏
        cv2.putText(frame, f"FPS: {fps:.1f} | Objects: {len(objects)}",
                    (10, frame.shape[0]-10), ...)

        cv2.imshow('Robot Vision Guidance', frame)
        if cv2.waitKey(1) & 0xFF == 27:  # ESC 退出
            break

    cap.release()
    cv2.destroyAllWindows()
```

- [ ] 录制演示视频或制作 PPT 展示

**交付物**：`main.py`（静态模式）+ `demo_realtime.py`（实时模式）+ 项目报告。

---

## 四、时间规划建议

| 阶段 | 内容 | 建议时间 |
|------|------|---------|
| 第1周 | 环境搭建、数据准备、相机标定 | 2-3天 |
| 第2周 | 图像预处理 + 目标检测 | 3-4天 |
| 第3周 | 透视校正 + 深度估计 | 3-4天 |
| 第4周 | 坐标变换 + 位姿计算 + 路径规划 | 3-4天 |
| 第5周 | 系统集成、调试、报告撰写 | 3-4天 |

---

## 五、关键技术参考

| 技术点 | OpenCV 关键字 | 参考资料 |
|--------|--------------|---------|
| 相机标定 | `calibrateCamera`, `findChessboardCorners` | OpenCV 官方教程 |
| 图像预处理 | `GaussianBlur`, `equalizeHist`, `Canny` | 《数字图像处理》冈萨雷斯 |
| 轮廓检测 | `findContours`, `minAreaRect` | OpenCV 文档 |
| 透视变换 | `getPerspectiveTransform`, `warpPerspective` | OpenCV 几何变换章节 |
| ArUco 位姿 | `cv2.aruco` 模块 | OpenCV ArUco 教程 |
| 深度估计 | `StereoSGBM`, 视差图 | OpenCV 立体视觉教程 |

---

## 六、评分要点预判

根据任务描述"目标检测、图像校正、深度信息、路径规划"四个要点，建议：

1. **目标检测**（30%）：方法选型合理，检测准确率展示
2. **图像校正**（25%）：畸变校正 + 透视校正，校正前后对比
3. **深度信息**（20%）：深度获取方法可行，结果合理
4. **路径规划**（15%）：路径合理，有可视化
5. **整体完成度 + 报告**（10%）：系统完整，文档清晰

---

## 七、常见坑

1. **光照敏感**：阈值分割对光照变化很敏感，尽量在均匀光照下拍摄，或使用自适应阈值
2. **棋盘格标定别省**：拍标定图时棋盘格要清晰、多角度、覆盖画面边缘
3. **深度误差**：单目测距精度有限，报告中要分析误差来源
4. **轮廓噪声**：务必做形态学处理 + 面积筛选，否则会检出很多假目标
5. **坐标方向**：注意 OpenCV 图像坐标系 y 轴朝下，和机器人坐标系可能不一致

---

按这个顺序从头到尾走下来，每个步骤都有明确的输入输出，调试时可以单独验证每个模块。祝顺利。
